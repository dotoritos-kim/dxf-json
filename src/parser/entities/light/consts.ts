export enum LightType {
  Distant = 1,
  Point = 2,
  Spot = 3,
}

export enum AttenuationType {
  None = 0,
  InverseLinear = 1,
  InverseSquare = 2,
}

export enum ShadowType {
  RayTrace = 0,
  ShadowMap = 1,
}
