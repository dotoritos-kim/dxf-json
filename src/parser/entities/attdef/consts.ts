export enum AttributeFlag {
    NONE = 0,
    INVISIBLE = 1,
    CONSTANT = 2,
    VERIFICATION_REQUIRED = 4,
    PRESET = 8,
}

export enum AttDefMTextFlag {
    MULTILINE = 2,
    CONSTANT_MULTILINE = 4,
}
